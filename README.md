# seterm

This is a TUI serial viewer intended for embedded developers. It allows for reading and writing of UART,
and more importantly allows for automatic firmware upload upon detecting a file change.

